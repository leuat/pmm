program test3;
var  
   index, b: byte; 
begin

	index:=#10;
	b:=#5;
	if (index>#1 or b=#3) then 
		writeln("TRUE",#0)
	else
		writeln("FALSE",#0);

	poke($D800, #0, #$F0);

	moveto(#20,#20, #$04);

	printnumber(b);

	b := peek($D800, #0);

	printnumber(b);

end.
