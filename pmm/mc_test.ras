program mc_test;
var  
   index: byte; 
	 mainChar: IncBin("charsets/test_mc.bin","$2800");
	 screen: IncBin("screens/screen1.bin");
begin
	//clearscreen( #$0D, SCREEN_COL_LOC); // green

	setmulticolormode();
	poke(VIC_DATA_LOC, #0, #$1A);


	//copyhalfscreen(screen, $0400 , #12, #1);
	copyfullscreen(screen + 1000, $D800); 
	copyfullscreen(screen, $0400); 
	
	
	poke(MULTICOLOR_CHAR_COL, #0, BLACK);
	poke(MULTICOLOR_CHAR_COL, #1, #1); 
	poke(MULTICOLOR_CHAR_COL, #2, #2);

	//poke(SCREEN_BG_COL, #0, BLACK);
	//poke(SCREEN_FG_COL, #0, BLACK);

	


end.
